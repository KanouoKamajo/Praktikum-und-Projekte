<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:padding="24dp">

    <!-- IAD-Logo oben -->
    <ImageView
        android:id="@+id/iad_logo"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:src="@drawable/iad_logo"
        android:contentDescription="@string/desc_iad_logo"
        android:adjustViewBounds="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="48dp" />

    <!-- Haupttitel -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:text="@string/onb_title"
        android:textColor="@color/text_black"
        android:textSize="26sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iad_logo" />

    <!-- 🔽 Nur für Stabilität/Responsiveness: Untertitel in ScrollView -->

    <!-- Weiter-Button -->

    <androidx.core.widget.NestedScrollView
        android:id="@+id/scroll_body"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:overScrollMode="ifContentScrolls"
        app:layout_constraintBottom_toTopOf="@id/btnWeiter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTitle">

        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="52dp"
            android:alpha="0.95"
            android:gravity="center"
            android:lineSpacingExtra="6dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:text="@string/onb_subtitle"
            android:textColor="@color/text_black"
            android:textSize="17sp" />
    </androidx.core.widget.NestedScrollView>

    <!-- Weiter-Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnWeiter"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/onb_next"
        android:textAllCaps="false"
        android:textColor="@color/iad_white"
        android:backgroundTint="@color/btn_green"
        app:cornerRadius="16dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/scroll_body"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomButtons" />

    <!-- 🔸 Neue horizontale Leiste für Abmelden + Überspringen -->
    <LinearLayout
        android:id="@+id/bottomButtons"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Abmelden -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLogout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:backgroundTint="@color/btn_red"
            android:paddingHorizontal="16dp"
            android:paddingVertical="6dp"
            android:text="Abmelden"
            android:textAllCaps="false"
            android:textColor="@color/iad_white"
            android:textSize="13sp"
            app:cornerRadius="12dp" />

        <!-- Überspringen -->
        <TextView
            android:id="@+id/tvSkip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="@string/onb_skip"
            android:textColor="@color/text_black"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>
